import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stanificentglobal.app',
  appName: 'Stanificent Pos Systen',
  webDir: 'dist'
};

export default config;
